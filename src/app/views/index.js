import 'React' from 'react';

class view extends React.Component {
  selectView(view") {
    switch (view) {
      case "start":

        break;
      default:

    }
  }

  render() {
    return (

    )
  }
}
